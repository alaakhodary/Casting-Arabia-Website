const Loading: React.FC = () => {
  return (
    <div className="flex items-center justify-center">
      <div className="h-[50px] w-[50px] animate-spin rounded-[50%] border-4 border-t-[#6371E0]"></div>
    </div>
  );
};

export default Loading;
